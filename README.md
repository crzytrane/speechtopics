```txt
npm install
npm run dev
```

Set `AI_GATEWAY_ID` in `wrangler.toml` or your Pages/Workers environment to the Cloudflare AI Gateway you want inference to run through. It defaults to `default`.

```txt
npm run deploy
```
